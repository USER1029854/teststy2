# NodeStoreH — 0xcaecc8a5aa285b211de5af16b2b756e2e48721f5 (UNVERIFIED)

Recovered by decompilation. Helper referenced by `FeeOwnerNode` (called via selector `0xbe95492b`).
Hardcodes the same three STYNODE NFT addresses (`embedded_constants.json`), so it participates in the
node-reward eligibility computation. Downstream of the token; cannot mint STY or move the pair.
